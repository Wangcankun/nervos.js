// const run = document.querySelector('[title=Run]')

// run.addEventListener('click', () => {
//   const runTabView = document.getElementById('runTabView')
//   const container = runTabView.querySelector('[class^=container]')
//   const subContainer = container.querySelector('[class^=subcontainer]')
//   const submit = document.createElement('button')
//   submit.textContent = 'Deploy to AppChain'
//   container.append(submit, subContainer)

// })
const detailBtn = document.querySelector('[class^=details]')
detailBtn.addEventListener('click', () => {


  const currentFile = app.getCurrentFile()
  // const details = document.querySelector('[class^=detailsJSON]')
  // const bytecodeBlock = details.childNodes[2].querySelector('pre')
})
